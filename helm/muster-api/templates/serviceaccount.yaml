{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "muster-api.serviceAccountName" . }}
  labels:
    {{- include "muster-api.labels" . | nindent 4 }}
{{- end }}
