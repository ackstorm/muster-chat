apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    {{- include "muster-api.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "muster-api.selectorLabels" . | nindent 4 }}
  ports: [{port: 80, targetPort: 8765}]
